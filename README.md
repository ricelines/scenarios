# scenarios

`scenarios` is the repo for ready-made Amber scenario compositions.

The first scenario here is:

- `amber/user-agent.json5`: a generic Matrix-connected Codex agent built from `codex-a2a`, `matrix-a2a-bridge`, and `matrix-mcp`
